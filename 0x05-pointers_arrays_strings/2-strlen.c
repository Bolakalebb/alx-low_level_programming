#include "main.h"

/**
 * _strlen - Returns the length of a string.
 * @str: An input string
 * Return: Nothing
 */
size_t _strlen(const char *s)
{
	size_t length = 0;

	while (*str++)
		length++;

	return (len);
}
